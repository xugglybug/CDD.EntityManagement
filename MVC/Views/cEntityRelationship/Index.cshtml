@model IEnumerable<MVC.Models.cEntityRelationship>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.entityA.ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.relType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.entityB.ID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.entityA.ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.relType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.entityB.ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
